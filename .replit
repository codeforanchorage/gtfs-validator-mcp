run = "bash setup.sh && python -m venv .venv 2>/dev/null; .venv/bin/pip install -q -r requirements.txt && .venv/bin/python server.py"
entrypoint = "server.py"

[nix]
channel = "stable-24_05"

[languages.python3]
pattern = "**/*.py"

[languages.python3.languageServer]
start = "pylsp"
