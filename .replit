run = "bash setup.sh && pip install -r requirements.txt && python server.py"
entrypoint = "server.py"

[nix]
channel = "stable-24_05"

[languages.python3]
pattern = "**/*.py"

[languages.python3.languageServer]
start = "pylsp"
